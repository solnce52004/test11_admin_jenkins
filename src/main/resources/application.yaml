server:
  address: 188.212.125.157
  port: 4444
  compression:
    enabled: true
  http2:
    enabled: true

#  ssl:
#    enabled: true
#    key-alias: ${SSL_KEY}
#    key-store: ${SSL_FILE}
#    key-store-type: PKCS12
#
#    key-password: ${SSL_PASSWORD}
#    key-store-password: ${SSL_PASSWORD}
#    trust-store-password: ${SSL_PASSWORD}

  error:
    whitelabel:
      enabled: false
